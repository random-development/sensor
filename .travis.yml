dist: xenial
language: go

go:
  - "1.12"

git:
  depth: 1

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

install: true

before_script:
  - env GO111MODULE=on make init

script:
  - make build
  - make test
