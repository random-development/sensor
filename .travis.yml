dist: xenial
language: go

go:
  - "1.12"

git:
  depth: 1

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

install: true

before_script:
  - make init

script:
  - make build
  - make test
